
int main(){
    a=(3+a)*b - b   * b;
    if(a+b > c){
        a = func(a*b+c-2,sort(4*2));
        for(a = 5; a<5; --i ){
            for(a = fun(5+a +8*b);func(4,8)>b*(4+2); i++) {
                a = -func(a * b + c - 2, sort(4 * 2));
                if(a+n){
                    a = k+d;
                    cout << a;
                }
                else{
                    a = -a;
                }
            }
        }
    }
    else{
        cout << a;
        a = -n;
        a++;
        --a;
    }
    return a*2+b;
}
